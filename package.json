{
  "name": "water-guardian-x",
  "version": "1.0.0",
  "private": true,
  "description": "Web build of Water Guardian X prepared for Capacitor",
  "scripts": {
    "cap:init": "npx cap init",
    "cap:add:android": "npx cap add android",
    "cap:copy": "npx cap copy",
    "cap:open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.8"
  }
}
